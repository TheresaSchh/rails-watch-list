<h1>Lists</h1>

<ul>
    <% @lists.each do |list| %>
      <li>
        <%= link_to list.name, list_path(list) %>
        <%= list.name %>
      </li>
    <% end %>

    <%= link_to "Create a new Item", new_list_path %>
</ul>
