<h1>See the <%= @list.name %></h1>

<%= link_to "Go back to List", lists_path %>

<h2>
  <ul>
    <% @list.movies.each do |movie| %>
      <li>
        <%= movie.title %>
      </li>
    <% end %>
  </ul>
</h2>

<%= link_to "remove a bookmark", bookmark_path(@bookmark),
    data: {turbo_method: :delete, tubo_confirm: "Sure?"} %>
